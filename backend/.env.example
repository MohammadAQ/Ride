# Backend configuration
NODE_ENV=development
PORT=8080

# Comma separated list of allowed origins for CORS, e.g. http://localhost:3000,http://localhost:8081
CORS_ORIGINS=

# Firebase Admin SDK credentials
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=service-account@example.iam.gserviceaccount.com
# Make sure to keep the new lines escaped when copying from the Firebase console.
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\\nYOUR-PRIVATE-KEY\\n-----END PRIVATE KEY-----\\n
