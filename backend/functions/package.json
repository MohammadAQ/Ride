{
  "name": "ride-notifications-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "deploy": "firebase deploy --only functions",
    "watch": "tsc -w"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.4.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
